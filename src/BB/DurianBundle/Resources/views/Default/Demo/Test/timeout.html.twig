{% extends 'BBDurianBundle:Default/Demo:index.html.twig' %}

{% block content %}
<div class="page-header">
    <h1>Test Request</h1>
    <code>GET /api/test/timeout</code>
</div>

<p class="lead">測試連線</p>
<p>可設定延遲幾秒以後再回傳，回傳包含系統相關資訊</p>

<form class="form-horizontal" role="form">
    <fieldset>
        <div class="form-group">
            <label class="control-label col-md-3">second</label>
            <div class="col-md-5">
                <div class="input-group">
                    <span class="input-group-addon">
                        <input class="action-enable" type="checkbox">
                    </span>
                    <input name="second" type="text" class="form-control" disabled>
                </div>
            </div>
            <span class="help-block col-md-4">延遲秒數(預設為5秒)</span>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button data-request-routing="api_test_timeout" class="btn btn-primary" type="button" data-loading-text="loading...">Request</button>
            </div>
        </div>
    </fieldset>
</form>
{% endblock %}
